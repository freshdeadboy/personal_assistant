# setup.py
from setuptools import setup, find_packages

setup(
    name='personal_assistant',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # додайте залежності, якщо є
    ],
)